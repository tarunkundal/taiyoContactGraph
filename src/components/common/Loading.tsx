import { Center, Spinner } from '@chakra-ui/react'

const Loading = () => {
    return (
        <Center h={'100vh'}>
            <Spinner>
            </Spinner>
        </Center>)
}

export default Loading